import os
import threading
import time
import random
from datetime import datetime

class animatronique:
    def __init__(self, name):
        self.name = name

def init_message(message):
    print(f"{message} -------------- OK")

def __init__():
    print("Initialisation ...")
    
    actuel = datetime.now()

    ts_actuel = actuel.timestamp()

    init_message('Récupération du temps')

__init__()